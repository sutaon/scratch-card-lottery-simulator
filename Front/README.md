# Optional Fonts

This directory is reserved for optional local font files.

Bundled system fonts are intentionally not committed. The app falls back to
available system fonts or Pillow's default bitmap font when custom files are
missing.
